Features
    Jogging
        Parameters
            Only enable Jog button when inputs are valid
        Joystick
            Buttons
                x+/-, y+/-, z+/-
                Feed
                Distance / Unit
                Cancel
    Stream
    Settings
    Script
    Keyboard navigation (tab / btab / arrows etc)
    Import server from https://github.com/fornellas/serialtcp
Improvements
    Limit Jogging to work area fetched from $$
    internal/
    Add line limits to various TextView
    TextView logging
        debug why with DisableGroupEmoji=false, it wrecks UI (it looks like there's double last rune for every line that there's an emoji)
    Scrolling
        Only scroll to end if view position is already at the end
            if not, change color to indicate there's new text
